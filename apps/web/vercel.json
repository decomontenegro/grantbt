{
  "buildCommand": "cd ../.. && pnpm install && pnpm db:generate && pnpm --filter=@grantbr/web build",
  "devCommand": "pnpm dev",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next"
}
